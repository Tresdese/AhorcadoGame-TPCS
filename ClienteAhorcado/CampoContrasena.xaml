<UserControl x:Class="ClienteAhorcado.CampoContrasena"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Grid>
        <PasswordBox x:Name="txtOculto" FontSize="14" Height="40" Padding="8,0,40,0"
                     VerticalContentAlignment="Center" BorderBrush="#CCCCCC" BorderThickness="1"
                     PasswordChanged="txtOculto_PasswordChanged">
            <PasswordBox.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="5"/>
                </Style>
            </PasswordBox.Resources>
        </PasswordBox>

        <TextBox x:Name="txtVisible" FontSize="14" Height="40" Padding="8,0,40,0"
                 VerticalContentAlignment="Center" BorderBrush="#CCCCCC" BorderThickness="1"
                 Visibility="Collapsed" TextChanged="txtVisible_TextChanged">
            <TextBox.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="5"/>
                </Style>
            </TextBox.Resources>
        </TextBox>

        <Button x:Name="btnOjito" Width="30" Height="30" HorizontalAlignment="Right" Margin="0,0,5,0" 
                Background="Transparent" BorderThickness="0" Cursor="Hand" Click="btnOjito_Click">
            <Image x:Name="imgOjito" Source="/Recursos/ver_contrasena.png" Width="20" Height="20" Opacity="0.6"/>
        </Button>
    </Grid>
</UserControl>